[tagpr]
  releaseBranch = main
  versionFile = README.md, update-deno-lock-file/action.yml
  vPrefix = true
  changelog = false
  templateText = Release: {{ .NextVersion }}\n{{ .Changelog }}
  majorLabels = 🧨 Breaking Change
  minorLabels = 🎉 New Feature
  commitPrefix = chore(release):
