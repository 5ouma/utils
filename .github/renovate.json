{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>5ouma/utils", "github>Omochice/renovate-config:deno#v4.1.1"],
  "pre-commit": { "enabled": true },
  "renovate-config": {
    "managerFilePatterns": ["/(^|/).?(renovate(?:rc)?|default)(?:\\.json5?)?$/"]
  },
  "customManagers": [
    {
      "customType": "jsonata",
      "fileFormat": "yaml",
      "managerFilePatterns": ["pre-commit/action.yml"],
      "matchStrings": [
        "$[0].runs.steps[env.PREK_VERSION].{\"currentValue\": env.PREK_VERSION}"
      ],
      "datasourceTemplate": "github-releases",
      "depNameTemplate": "j178/prek"
    },
    {
      "customType": "regex",
      "managerFilePatterns": [".pre-commit-config.yaml"],
      "matchStrings": [
        "github\\.com/(?<depName>[^/]+/[^\\s/]+)(?:/[^@]+)?@(?<currentDigest>[0-9a-f]{40}) # frozen: (?<currentValue>v[0-9]+\\.[0-9]+\\.[0-9]+)"
      ],
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver",
      "autoReplaceStringTemplate": "{{{replace 'v[0-9]+\\.[0-9]+\\.[0-9]+' newValue (replace '@[0-9a-f]{40}' (replace '^' '@' newDigest) (replace '/v[0-9]+' (replace '^' '/v' (toJSON newMajor)) replaceString))}}}"
    }
  ]
}
